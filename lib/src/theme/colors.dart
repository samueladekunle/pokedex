import 'package:flutter/material.dart';

class PokedexColorLight {
  const PokedexColorLight._();

  static const primaryColor = Color(0xFF3558CD);
  static const scaffoldBackgroundColor = Color(0xFFE8E8E8);

  /* ---------- Text ----------- */
  static const lightTextColor = Color(0xFF6B6B6B);
  static const darkTextColor = Color(0xFF161A33);

  /* ---------- Labels ---------- */
  static const selectedLabelColor = Color(0xff161A33);
  static const unselectedLabelColor = Color(0xff6B6B6B);
  // static const badgeColor = primaryColor;

  /* ---------- Indicators ------------ */
  static const lowStatIndicatorColor = Color(0xffCD2873);
  static const mediumStatIndicatorColor = Color(0xffEEC218);
  static const highStateIndicatorColor = Colors.green;

  /* ------------ Button colors ----------- */
  static const lightButtonColor = Color(0xffD5DEFF);
}
